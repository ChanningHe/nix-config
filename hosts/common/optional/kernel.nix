# Kernel configuration options
# NOTE(starter): Add custom kernel configurations as needed
{ ... }:
{
  # Add kernel-specific configurations here
  # Example:
  # boot.kernelParams = [ "some.param=value" ];
}
