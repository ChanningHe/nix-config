# Minimal NixOS-specific configuration for service user rl-man
{
  # Service users typically don't need complex NixOS-specific home-manager configurations
  # Keep this file minimal and focused on essential system integration only
}
